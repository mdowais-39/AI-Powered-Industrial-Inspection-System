[package]
name = "measurement"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
opencv = "0.98.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
pipe_core = { path = "../pipe_core" }
